# Train/val/test sets as
## 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]


#path: ../datasets/coco128  # dataset root dir
train: ./data/metrics  # metrics images (relative to 'path') 128 images
val: ./data/val # val images (relative to 'path') 128 images
test: ./data/test # test images

# Classes
nc: 3  # number of classes
names: ["without_mask", "with_mask", "mask_weared_incorrect",]  # class names